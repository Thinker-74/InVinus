# Checkpoint — 2026-03-29 00:41

**Progetto:** InVinus — /home/michele/Progetti/InVinus
**Branch:** main

## Task in corso
Sviluppo backoffice web — Fase 1 quasi completa

## Completato
- Fix logica provvigioni in `get_dashboard_consulente`: guadagno_personale = PV×15% (non fatturato×15%), residuale per livello da tabella qualifiche
- Fix build produzione: Next.js 16 + NODE_ENV=development durante build causa crash React context nel prerender — soluzione: `npm run build` senza override NODE_ENV
- Aggiunti `global-error.tsx` e `not-found.tsx` per gestire le pagine interne Next.js 16
- Pagina `/team`: albero downline raggruppato per livello (L1 Diretti, L2, L3...) con badge attivo/inattivo, barra PV, "via [sponsor]" sui livelli > 1
- Pagina `/ordini`: lista ultimi 30 ordini + form "Nuovo ordine" con righe dinamiche e calcolo PV real-time
- RPC `get_team_consulente`: CTE ricorsiva SECURITY DEFINER, 8 livelli
- RPC `crea_ordine_consulente`: SECURITY DEFINER, inserisce ordine + righe, calcola totale/PV da prodotti
- Fix bug 1 ordini: `JSON.stringify(righe)` passava stringa invece di array JSONB al RPC
- Fix bug 2 ordini: `FOR v_riga IN SELECT value FROM jsonb_array_elements(...)` → "missing FROM-clause entry" — risolto con `FOREACH v_riga IN ARRAY (array_agg(...))`

## Modifiche in corso
Nessuna — tutto committato e pushato

## Decisioni prese
- Ionos 1&1 (hosting esistente) è ininfluente: frontend va su Vercel, DB su Supabase
- FOREACH su JSONB in PL/pgSQL: usare `SELECT array_agg(value) INTO arr FROM jsonb_array_elements(json_param)` poi `FOREACH v IN ARRAY arr`
- `supabase gen types typescript --linked` va eseguito dalla root del progetto (non da `web/`)
- Parametri JSONB a RPC Supabase: passare oggetti/array JS direttamente, non `JSON.stringify`

## Prossimi passi
- Pagina `/clienti`: lista clienti del consulente
- Pagina `/provvigioni`: storico provvigioni mensili
- Pagina `/catalogo` miglioramenti (dettaglio prodotto?)
- Edge Function `calcola-provvigioni` (batch mensile)
- RLS più granulare (ogni consulente vede solo i propri dati)
- Deploy su Vercel con dominio Ionos puntato

## Blocchi / Note
- Il middleware Next.js ha un deprecation warning (middleware → proxy) — non bloccante
- RLS INSERT su `ordini` e `ordini_righe` non necessaria: gestita via SECURITY DEFINER
