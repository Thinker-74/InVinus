# Checkpoint — 2026-03-30 01:30

**Progetto:** InVinus
**Branch:** main

## Task in corso
- Pannello admin completo + navigazione sidebar basata su ruolo

## Completato
- **Pannello admin — 4 pagine:**
  - `/admin/dashboard` — KPI globali, trend 6 mesi, top 5 consulenti
  - `/admin/consulenti` — lista rete con filtri per status/attività, click → dettaglio downline
  - `/admin/candidature` — revisione candidature, approva (crea consulente STARTER) / rifiuta con nota
  - `/admin/provvigioni` — selettore periodo, bottone calcola batch, tabella risultati, bottone approva
- **Route Handler** `/api/admin/calcola-provvigioni` — chiama motore TypeScript esistente via pg Pool (richiede `DATABASE_URL` in `.env.local`)
- **Engine TypeScript copiato** in `web/src/lib/provvigioni/` per importarlo dal web app
- **pg aggiunto** come dipendenza del web app
- **Sidebar refactoring completo:**
  - Legge ruolo da Supabase al mount (non più cookie) → admin vede sezione Amministrazione immediatamente
  - Sezione "Amministrazione" con sfondo/testo rosso + icona scudo
  - Badge "Admin" + nome/cognome nel footer utente
  - Label voci admin aggiornate: Dashboard Admin, Gestione Consulenti, Candidature, Calcolo Provvigioni
- **Banner visivo** rosso in cima a tutte le pagine `/admin/*`
- **Fix `.npmrc`** — `include=dev` per installare devDependencies anche con `NODE_ENV=production`
- **Fix pre-render** su `/referral/gestisci` — aggiunto `export const dynamic = "force-dynamic"`

## Modifiche in corso
- Nessuna (tutto committato)

## Decisioni prese
- Ruolo admin letto da Supabase al mount sidebar (non cookie) — più affidabile, nessun chicken-and-egg
- Engine TypeScript copiato in `web/src/lib/provvigioni/` invece di referenziare il package root — evita problemi tsconfig
- `DATABASE_URL` richiesto in `.env.local` per il calcolo batch — non ancora configurato in produzione
- `npm run build` senza NODE_ENV esplicito — Next.js lo gestisce internamente; `NODE_ENV=development npm run build` causa errori pre-rendering

## Prossimi passi
- Aggiungere `DATABASE_URL` a `.env.local` e su Vercel (connection string Supabase → Settings → Database → URI)
- Testare flusso completo: candidatura → approvazione → consulente creato
- Testare calcolo batch provvigioni con dati reali
- Valutare se aggiungere pagina `/admin/ordini` (lista ordini globale)
- Push su GitHub / deploy Vercel

## Blocchi / Note
- `tailwindcss-postcss-4.2.2.tgz` nella root web (artifact di `npm pack`) — da aggiungere a .gitignore o eliminare
- Il calcolo batch non funzionerà senza `DATABASE_URL` — mostrerà errore "DATABASE_URL non configurato"
