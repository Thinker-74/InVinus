# Checkpoint — 2026-03-28 21:00

**Progetto:** InVinus
**Branch:** main (git non ancora inizializzato)

## Task in corso
- Setup iniziale del progetto: schema PostgreSQL + servizio calcolo provvigioni mensili

## Completato
- Schema PostgreSQL completo (`db/schema.sql`, 729 righe):
  - 14 enum types, 19 tabelle, 22 indici
  - Seed dati: 4 regioni, 4 cantine fornitrici, 30 prodotti catalogo 2026, 9 qualifiche compensation plan, 5 consulenti demo (albero 3 livelli), 4 clienti, 3 box degustazione
- Servizio TypeScript calcolo provvigioni mensili (`src/jobs/calcoloProvvigioni/`):
  - `types.ts` — interfacce, enums, costanti
  - `engine.ts` — 6 step puri (nessun I/O), orchestratore `eseguiBatch()`
  - `repository.ts` — query PostgreSQL con UPSERT idempotente
  - `index.ts` — entry point con transazione unica e rollback
- Test suite (`src/__tests__/calcoloProvvigioni.test.ts`): **46/46 passati**
  - Scenari: albero seed, storni, promozioni, intermezzo formativo, CAB, soglia payout, Global Pool, Bonus Car, edge cases
- Configurazione progetto: `package.json`, `tsconfig.json`, `jest.config.js`

## Modifiche in corso
- Tutti i file sono nuovi (nessun commit ancora):
  - `db/schema.sql`
  - `src/jobs/calcoloProvvigioni/types.ts`
  - `src/jobs/calcoloProvvigioni/engine.ts`
  - `src/jobs/calcoloProvvigioni/repository.ts`
  - `src/jobs/calcoloProvvigioni/index.ts`
  - `src/__tests__/calcoloProvvigioni.test.ts`
  - `package.json`, `tsconfig.json`, `jest.config.js`

## Decisioni prese
- TypeScript (non Python): i doc citano già file `.ts`
- Attività mensile basata su PV only (doc 12 §3), non PV+GV (doc 02 regola 2 — più restrittiva)
- Provvigione personale pagata anche se inattivo (doc 12 §8 suggerimento #4: SÌ)
- Promozione sequenziale: un livello alla volta
- Bonus Car = 250€/mese placeholder (DA DECIDERE con FP — doc 12 §6)
- Soglia minima payout = 10€ (doc 12 §8 #8)
- 1 PV ≈ 1€ nei test (conversione reale da definire con FP)
- `eseguiBatch()` è pura (in-memory), `index.ts` aggiunge la persistenza

## Prossimi passi
- Inizializzare git + creare .gitignore
- Creare memory files del progetto
- API REST (Express/Fastify) per i moduli CRM, ordini, consulenti
- Modello dati e logica magazzino/cantina
- Auth JWT + ruoli (admin, consulente, cliente)
- Integrazione Stripe per pagamenti

## Blocchi / Note
- Bonus Car importo non ancora definito con Francesco Panzella
- Conversione €→PV da formalizzare (attualmente 1:1 nei test)
- Decisioni "[DA DECIDERE]" nel doc 12: PV al pagamento o consegna, storno PV resi >14gg, visibilità dati albero — non bloccanti per il batch, ma da decidere prima del lancio
