# Checkpoint — 2026-03-30 00:15

**Progetto:** InVinus
**Branch:** main

## Task in corso
- Risoluzione Security Advisor warnings Supabase (5 advisory)

## Completato
- `function_search_path_mutable` (3/3): aggiunto `SET search_path = public` via ALTER FUNCTION su `get_team_consulente`, `get_dashboard_consulente`, `crea_ordine_consulente`
- `extension_in_public`: pg_trgm spostata in schema `extensions` (standard Supabase); indice GIN `idx_prodotti_nome_trgm` intatto (legato a OID)
- `db/schema.sql` aggiornato: `CREATE SCHEMA IF NOT EXISTS extensions` + `CREATE EXTENSION IF NOT EXISTS pg_trgm SCHEMA extensions`
- `db/migrations/001_security_fixes.sql` creato per documentare i fix applicati al DB

## Modifiche in corso
- `db/schema.sql` — aggiornato per riflettere schema `extensions` per pg_trgm (non committato)
- `db/migrations/001_security_fixes.sql` — nuovo file con ALTER FUNCTION e migrazione estensione (non committato)

## Decisioni prese
- `auth_leaked_password_protection` (HIBP): non risolvibile — richiede piano Pro Supabase. Advisory ignorato consapevolmente sul free tier.
- Le 3 funzioni SECURITY DEFINER usano `SET search_path = public` (non `public, extensions`) perché nessuna usa operatori trgm direttamente

## Prossimi passi
- Commit + push dei file modificati
- Procedere con i prossimi task di sviluppo (da definire con l'utente)

## Blocchi / Note
- 4/5 advisory risolti; il 5° (HIBP) è bloccato da limite piano free
